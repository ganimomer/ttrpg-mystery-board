import styled from "styled-components";

/** Whatever the app has to say while it has no board to show. */
export const StyledCenterMessage = styled.div`
  height: 100%;
  display: flex;
  flex-direction: column;
  gap: 14px;
  align-items: center;
  justify-content: center;
  color: #f0e6d5;
  font-size: 18px;
`;
